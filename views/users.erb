<div class="row">
  <div class="col-xl-2">
    <p>Click on column headings to sort by that column. Click again to reverse the sort order.</p>
    <p>Click 'Details' to view more information about a user.</p>
  </div>
  <div class="col-xl-10">
    <table>
      <thead>
        <tr>
          <% @sort_links.each do |sort_link| %>
            <th><%= sort_link %></th>
          <% end %>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
        <tr>
          <td><%= user.username %></td>
          <td><%= user.num_of_ascents %></td>
          <td><a class="btn btn-outline-secondary btn-sm" href="/users/<%= user.id %>">Details</a></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>